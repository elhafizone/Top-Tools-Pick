-- Editorial (pre-authored) comparisons were replaced by the interactive comparison
-- builder at /compare, which composes comparisons from published products at request
-- time. The Comparison table, its admin screens and its public route are all removed.

DROP TABLE IF EXISTS "Comparison";
