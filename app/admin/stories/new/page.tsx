import { requireAdmin } from "@/lib/admin/auth";
import { FormError } from "@/components/admin/FormError";
export default async function NewStoryPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { await requireAdmin(); const { error } = await searchParams; return <section><h1 className="text-3xl font-black">New story</h1><FormError message={error} /><form action="/api/admin/stories" method="post" className="mt-6 grid max-w-2xl gap-4"><input name="title" required placeholder="Title" className="border px-3 py-2" /><input name="slug" required placeholder="Slug" className="border px-3 py-2" /><textarea name="description" required placeholder="Description" className="border px-3 py-2" rows={4} /><input name="posterImage" placeholder="Poster image URL" className="border px-3 py-2" /><input name="mediaUrl" placeholder="First page media URL" className="border px-3 py-2" /><button className="w-fit bg-slate-950 px-5 py-3 text-sm font-semibold text-white">Create draft</button></form></section>; }
