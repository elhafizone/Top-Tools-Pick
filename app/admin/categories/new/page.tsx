import { requireAdmin } from "@/lib/admin/auth";
export default async function NewCategoryPage() { await requireAdmin(); return <section><h1 className="text-3xl font-black">New category</h1><form action="/api/admin/categories" method="post" className="mt-6 grid max-w-2xl gap-4">{["name", "slug", "description", "seoTitle", "seoDescription"].map((name) => <label key={name} className="text-sm font-semibold">{name}<input name={name} className="mt-2 block w-full border border-slate-300 px-3 py-2" /></label>)}<button className="w-fit bg-slate-950 px-5 py-3 text-sm font-semibold text-white">Create category</button></form></section>; }
